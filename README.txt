OBJ2PNG Studio Pro v1.0.0

Windows x64 standalone release.
The custom OBJ -> PNG icon is embedded directly into the executable's PE resources, so Windows Explorer, shortcuts, the title bar and taskbar can all use the application icon.
